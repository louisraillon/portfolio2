// components/ProjectModal.tsx (stub — will be replaced)
'use client'
import type { Project } from '@/lib/projects'
type Props = { project: Project | null; onClose: () => void }
export default function ProjectModal({ project, onClose }: Props) {
  return null
}
